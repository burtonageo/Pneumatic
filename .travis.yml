language: cpp
compiler:
  - gcc
  - clang

install:
  - git submodule init
  - git submodule update

before_script:
  - mv CmakeLists.txt CMakeLists.txt
  - mkdir build
  - cd build
  - cmake ..

script: make
