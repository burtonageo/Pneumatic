#version 330 core

uniform sampler2D texSampler;

in Vertex {
  vec4 color;
  vec2 texCoord;
} IN;

out vec4 fragColor;

void main(void)
{
  fragColor = IN.color * texture(texSampler, IN.texCoord);
}
